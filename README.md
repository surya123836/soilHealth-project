# soilHealth-project
To run frontend:->npm install
                ->npm start
To install backend:->npm init -y
               ->npm install express mongoose cors
To run backend:->node server.js
               ->npm start
